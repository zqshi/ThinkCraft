# 质量检查清单

Version: 1.0.1
Last Updated: 2026-01-28

## 概述

本文档定义了Agent调度链路中的质量检查清单，用于确保Agent调用和输出的质量。

```markdown
- [ ] 关键检查项示例
```

## 一、调用前检查（协调者使用）

### 1.1 职责边界扫描

扫描Prompt是否包含以下违规模式，如有则自动删除：

#### 🔴 模式A：指令性列表

- **正则**：`(应该|应当|需要|必须).{0,20}(包含|涵盖|分为).{0,10}[:：]`
- **违规示例**："应该包含以下内容："
- **处理方式**：删除列表，替换为"请运用你的专业方法论完成任务"

#### 🔴 模式B：结构预设

- **正则**：`(章节结构|内容结构|输出结构|文档结构).{0,10}(为|是|包括)`
- **违规示例**："章节结构为..."
- **处理方式**：删除整个结构描述段落

#### 🔴 模式C：维度枚举

- **正则**：`(维度|方面|角度).{0,10}[:：]\s*\n\s*[-*\d]`
- **违规示例**："从以下维度分析：\n- xxx"
- **处理方式**：删除列表部分

#### 🔴 模式D：按照XX组织

- **正则**：`按照.{0,20}(顺序|结构|方式|逻辑)`
- **违规示例**："按照市场-竞品-用户的顺序"
- **处理方式**：删除整句

#### 🔴 模式E：关注/注意类暗示

- **正则**：`(重点|特别|优先|需要|请)(关注|注意|考虑|聚焦).{0,10}[:：]\s*\n\s*[-*\d]`
- **违规示例**："重点关注：\n- xxx"
- **处理方式**：删除列表部分

#### 🔴 模式F：读取说明中的内容暗示

- **正则**：`读取说明.{0,50}(重点|关注|注意).{0,20}[:：]`
- **违规示例**："读取说明：请使用Read工具读取此文件，重点关注："
- **处理方式**：删除"重点关注"及后续列表

**检查流程**：

1. 提取Prompt中的"## 本次任务"和"### 任务依赖信息"章节
2. 运行正则扫描，匹配模式A/B/C/D/E/F
3. 如发现匹配：标记为❌，输出违规内容，自动修正，重新扫描
4. 标记为✅ 职责边界检查通过

### 1.2 5W1H信息完整性检查

- **Who**：是否明确告知Agent它是谁？职责边界是否清晰？
- **Where**：是否说明当前在整体流程的哪个阶段？
- **What**：任务描述是否具体、可执行、边界清晰？
- **Why**：是否说明为什么调用这个Agent？输出将如何被使用？
- **When**：是否说明这是第几次调用？上一轮做了什么？
- **How**：是否提供了完成任务所需的全部信息？依赖文件路径是否明确？

**关键检查**：

- [ ] 信息完整：用户原始需求是否完整复制到prompt？
- [ ] 任务可执行：避免"与用户对话"、"等待用户回复"等不可能完成的表述
- [ ] 依赖链完整：第N轮是否包含第1轮的所有依赖文件？
- [ ] 依赖传递纯净：读取说明是否保持纯净，不包含内容提示？

### 1.3 违规模式深度扫描

- **对话暗示类**：是否包含"与用户对话"、"向用户提问"、"等待用户回复"？
- **职责越界类**：是否要求Agent"调用其他Agent"、"与用户交互"？
- **上下文缺失类**：多轮调用时，是否说明这是第几次调用？是否提供完整历史上下文？
- **输出要求不明确类**：是否明确输出类型、格式、路径、命名、合规要求？

**自检结论**：✅ 三层自检全部通过，开始调用{agent_name}

**🔴 如任何一项未通过，必须修正prompt后再调用。**

---

## 二、调用后检查（协调者使用）

### 2.1 角色混淆检测

扫描Agent输出是否包含：

- "我应该调用"、"我作为协调者"
- "我需要等待用户回复"
- "让我直接与用户沟通"

**如发现**：在下次Prompt中明确：输出对象是协调者、使用报告式语言、Agent不直接与用户对话。

### 2.2 信息透传验证

**强制要求**：原封不动地将Agent的完整输出呈现给用户。

**禁止行为**：

- ❌ 使用"Agent已经..."、"Agent提出了X个问题"等概括性描述
- ❌ 摘要、改写、删减Agent的输出
- ❌ 替Agent做决策或补充内容

**正确做法**：

- ✅ 完整复制Agent的输出
- ✅ 保持原有格式和结构
- ✅ 如需说明，在输出前后添加协调者的说明，但不修改Agent输出本身

### 2.3 输出质量验证

- [ ] 输出是否符合要求的类型和格式？
- [ ] 输出是否包含必要的元信息（如果是文档）？
- [ ] 输出是否完整，没有截断或遗漏？
- [ ] 输出是否可以被下一个Agent使用（如果有依赖）？

---

## 三、文档输出检查（Agent使用）

### 3.1 模板匹配检查

- [ ] 是否使用了指定的文档模板？
- [ ] 模板路径是否正确？
- [ ] 模板版本是否最新？

### 3.2 元信息完整性检查

**必填字段**：

- [ ] Template：使用的模板路径
- [ ] Version：文档版本号
- [ ] Changelog：变更日志
- [ ] Created：创建时间
- [ ] Updated：最后更新时间

**可选字段**：

- [ ] Author：作者
- [ ] Dependencies：依赖文件列表
- [ ] Status：文档状态（草稿/审核中/已确认）

### 3.3 目录与命名规范检查

- [ ] 文件路径是否符合规范？
- [ ] 文件命名是否符合约定？
- [ ] 目录结构是否正确？

### 3.4 依赖/来源检查

- [ ] 依赖文件是否存在？
- [ ] 依赖文件路径是否正确？
- [ ] 来源信息是否完整？

### 3.5 用户确认检查

- [ ] 是否已获得上一阶段的用户确认？
- [ ] 用户确认的内容是否已纳入本次输出？
- [ ] 是否需要等待用户确认才能进入下一阶段？

### 3.6 核心原则符合性检查

- [ ] 是否符合大模型原生应用核心原则？
- [ ] 是否符合Agent协作设计规范？
- [ ] 是否符合产品开发核心原则？

---

## 四、代码输出检查（Agent使用）

### 4.1 代码质量检查

- [ ] 代码是否可以运行？
- [ ] 代码是否有语法错误？
- [ ] 代码是否符合项目的编码规范？
- [ ] 代码是否有适当的注释？

### 4.2 功能完整性检查

- [ ] 是否实现了所有要求的功能？
- [ ] 是否处理了边界情况？
- [ ] 是否有错误处理？

### 4.3 测试覆盖检查

- [ ] 是否包含单元测试？
- [ ] 测试是否覆盖主要功能？
- [ ] 测试是否可以通过？

### 4.4 依赖管理检查

- [ ] 是否正确声明了依赖？
- [ ] 依赖版本是否兼容？
- [ ] 是否有不必要的依赖？

---

## 五、Agent产品特有检查

### 5.1 Prompt质量检查

- [ ] Prompt是否清晰明确？
- [ ] Prompt是否包含必要的上下文？
- [ ] Prompt是否避免了歧义？
- [ ] Prompt是否符合大模型的理解能力？

### 5.2 工具设计检查

- [ ] 工具定义是否清晰？
- [ ] 工具参数是否完整？
- [ ] 工具返回值是否明确？
- [ ] 工具是否可以被Agent正确调用？

### 5.3 用户用例检查

- [ ] 用户用例是否覆盖主要场景？
- [ ] 用户用例是否可测试？
- [ ] 用户用例是否有明确的预期结果？

### 5.4 成本优化检查

- [ ] Token消耗是否在合理范围？
- [ ] 是否有不必要的冗余信息？
- [ ] 是否可以通过优化Prompt减少Token消耗？

---

## 六、快速检查清单

### 协调者调用前快速检查

```
✅ 职责边界扫描通过
✅ 5W1H信息完整
✅ 违规模式扫描通过
✅ 依赖信息完整
✅ 输出要求明确
```

### Agent输出后快速检查

```
✅ 无角色混淆
✅ 输出完整
✅ 格式正确
✅ 元信息完整（如果是文档）
✅ 可被下游使用
```

### 文档输出快速检查

```
✅ 使用正确模板
✅ 元信息完整
✅ 目录命名规范
✅ 依赖路径正确
✅ 已获用户确认
✅ 符合核心原则
```

---

## 七、检查工具

### 7.1 自动化检查脚本

```javascript
// 职责边界扫描
function scanBoundaryViolations(prompt) {
  const patterns = [
    /(应该|应当|需要|必须).{0,20}(包含|涵盖|分为).{0,10}[:：]/g,
    /(章节结构|内容结构|输出结构|文档结构).{0,10}(为|是|包括)/g,
    /(维度|方面|角度).{0,10}[:：]\s*\n\s*[-*\d]/g,
    /按照.{0,20}(顺序|结构|方式|逻辑)/g,
    /(重点|特别|优先|需要|请)(关注|注意|考虑|聚焦).{0,10}[:：]\s*\n\s*[-*\d]/g,
    /读取说明.{0,50}(重点|关注|注意).{0,20}[:：]/g
  ];

  const violations = [];
  patterns.forEach((pattern, index) => {
    const matches = prompt.match(pattern);
    if (matches) {
      violations.push({
        pattern: `模式${String.fromCharCode(65 + index)}`,
        matches: matches
      });
    }
  });

  return violations;
}

// 5W1H检查
function check5W1H(prompt) {
  const checks = {
    who: prompt.includes('你是') || prompt.includes('Agent身份'),
    where: prompt.includes('当前阶段') || prompt.includes('上下文'),
    what: prompt.includes('任务目标') || prompt.includes('本次任务'),
    why: prompt.includes('调用原因') || prompt.includes('输出将'),
    when: prompt.includes('第') && prompt.includes('轮'),
    how: prompt.includes('依赖信息') || prompt.includes('完成任务所需')
  };

  return checks;
}
```

### 7.2 手动检查模板

```markdown
## 调用前检查表

### 职责边界扫描

- [ ] 模式A：指令性列表
- [ ] 模式B：结构预设
- [ ] 模式C：维度枚举
- [ ] 模式D：按照XX组织
- [ ] 模式E：关注/注意类暗示
- [ ] 模式F：读取说明中的内容暗示

### 5W1H检查

- [ ] Who：Agent身份明确
- [ ] Where：当前阶段说明
- [ ] What：任务目标具体
- [ ] Why：调用原因清晰
- [ ] When：调用轮次说明
- [ ] How：依赖信息完整

### 违规模式扫描

- [ ] 无对话暗示
- [ ] 无职责越界
- [ ] 上下文完整
- [ ] 输出要求明确

### 最终确认

- [ ] 所有检查项通过
- [ ] 可以开始调用Agent
```

---

## 八、版本信息

- **版本**：1.0.0
- **最后更新**：2026-01-27
- **变更日志**：
  - 1.0.0：初始版本，定义质量检查清单
