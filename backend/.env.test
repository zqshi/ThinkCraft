# 测试环境配置
# 此文件可以提交到 Git

NODE_ENV=test
PORT=3001
FRONTEND_URL=http://localhost:8080

# DeepSeek API 配置（测试时使用 Mock）
DEEPSEEK_API_KEY=test_api_key
DEEPSEEK_API_URL=http://127.0.0.1:9999

# PostgreSQL数据库配置（测试数据库）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=thinkcraft_test
DB_USER=postgres
DB_PASSWORD=test_password

# JWT 配置
JWT_SECRET=test_jwt_secret_for_testing_only
JWT_EXPIRES_IN=1h

# 日志级别
LOG_LEVEL=error
